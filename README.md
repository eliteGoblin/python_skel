# Python Project Skeleton

Personal Python3 project template

Local development:
```s
sudo rm -rf .venv
python3 -m venv .venv
source .venv/bin/activate
which python
```


Run all CI scripts:

```sh
scripts/ci/ci
```
